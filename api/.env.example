# Copy to .env and fill in values.
# Loaded automatically by python-dotenv when running `fastapi dev`.

# PostgreSQL connection string — Dagster reverse-ETL writes the gold layer here.
DATABASE_URL=postgresql://user:password@localhost:5432/exercise_book
